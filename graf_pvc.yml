apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "grafana-pvc"
  labels:
    name: "grafana-volume"
spec:
  storageClassName: manual
  accessModes:
   - ReadWriteOnce
  resources:
     requests:
       storage: 1Gi

